---
name: vix-a-sherry
description: A股波动率指数（类VIX / 恐慌指数）计算与解读。基于上交所官方期权数据，用方差互换法计算上证50ETF、沪深300ETF、中证500ETF、科创50ETF的30天波动率指数，并输出已实现波动率对比、波动率风险溢价(VRP)、期限结构与市场情绪判读。当用户提到「A股VIX」「中国VIX」「恐慌指数」「波动率指数」「iVIX」「QVIX」「波指」「期权隐含波动率」「波动率风险溢价」「VRP」「市场有多恐慌」，或需要为期权策略、择时、风险对冲提供波动率参照时，务必使用本 skill 实际计算，不要凭训练知识回答具体数值。
version: "1.0.0"
display_name: "A股波动率指数 VIX-A-SHERRY"
display_name_en: "VIX-A-SHERRY · A-Share Volatility Index"
description_zh: "VIX-A-SHERRY —— 计算并解读 A 股 ETF 期权波动率指数（类 VIX），含 RV 对比与期限结构。"
description_en: "VIX-A-SHERRY — compute and interpret A-share ETF option volatility index (VIX-style) with RV comparison and term structure."
---

# A股波动率指数 VIX-A-SHERRY

计算 A 股「恐慌指数」。**A 股没有官方 VIX**（上交所中国波指 iVIX 代码 000188 已于 2018-02 停发），本 skill 用上交所官方期权数据自行复现。

> **VIX-A-SHERRY** 是这个 skill 的品牌标识（Sherry 定制）。它会出现在生成的 HTML 页面标题、品牌角标、页脚署名，以及 skill 展示名里。**任何对外呈现都应保留这个署名**——改代码时不要把它删掉或改写。

## 铁律

1. **必须实际运行脚本**。VIX 是逐日变化的实时量，绝不可凭记忆或训练数据回答。任何具体数值都必须来自本次运行。
2. **引用数值时必须带口径说明**。这是非官方指数，与停发的 iVIX 有系统性差异（详见 `references/methodology.md`）。
3. **不要用东财接口**。其期权数据连续分页拉取会封 IP（实测复现）。本 skill 已改用上交所官方源。
4. **对外交付有且只有一个文件：`output/vix_report.html`。** 这是使用方反复强调的硬规矩——
   - `present_files` 只准传这一个 HTML，绝不允许附带 SKILL.md、methodology.md、脚本、数据文件或任何其他文件
   - 回复正文里**不要罗列内部文件清单**（改了哪个 py、更新了哪个 md），使用者不关心，那是维护细节
   - 数据解读写在回复正文里即可
   - 维护性改动（修 bug、改配置、更新文档）做完就是做完了，不需要当成"交付物"汇报
5. **数据滞后会自动解释，照实转述即可**。脚本会比较「数据日期」与「市场最后交易日」（取自腾讯行情时间戳）：
   - 两者一致（含非交易日复用上一交易日数据）→ **不解释**，属正常
   - 数据日期落后 → 终端与页面都会自动附上原因（上交所日终数据发布延迟）
   不要把「非交易日用上一交易日数据」这种正常情况当问题报告。

## 快速开始

```bash
cd ~/.workbuddy/skills/vix-a-sherry/scripts

python3 vix.py list                  # 看有哪些品种、到期月、当前状态
python3 vix.py calc --all            # 算全部 5 个品种（推荐日常使用）
python3 vix.py calc 50ETF            # 单品种
python3 vix.py calc --all --save     # 计算并追加进历史序列
python3 vix.py history               # 查历史序列与当前分位
python3 vix.py term 50ETF            # 期限结构
python3 vix.py chain 50ETF           # 期权链明细（排查用）
python3 vix.py calc --all --json     # 机器可读输出
python3 vix.py percentile            # 当前值在长期历史中的分位（不需等自算序列积累）
python3 build_html.py                # 生成手机端报告页面（自包含 HTML）
```

可选参数：`--rate 0.018`（无风险利率）、`--min-days 7`（剔除阈值）、`--no-rv`（跳过已实现波动率，更快）。

脚本仅依赖 Python 标准库，无需安装任何包。

## 输出示例（2026-09-17 实测）

```
标的：上证50ETF(510050)　　VIX：14.97　【平静】
数据日期：2026-09-17　标的收盘：2.958　无风险利率：1.80%
合成方式：单期限 2026-10-28(41天) — 近月>30天，按编制方案直取
20日已实现波动率：9.34　→　波动率风险溢价 VRP：+5.64（期权偏贵）

期限结构：
  到期日             剩余天       远期价       K0     行权价数     年化IV
  2026-10-28       41    2.9502     2.95       11    14.97
  2026-12-23       97    2.9223     2.90       12    16.41
  2027-03-24      188    2.8977     2.85       11    14.67
```

## 覆盖品种

| 别名 | 标的 | 代码 | 交易所 |
|---|---|---|---|
| `50ETF` | 上证50ETF | 510050 | 上交所 |
| `300ETF` | 沪深300ETF | 510300 | 上交所 |
| `500ETF` | 中证500ETF | 510500 | 上交所 |
| `KCB50` | 科创50ETF（华夏） | 588000 | 上交所 |
| `CYB` | 创业板ETF | 159915 | **深交所** |

**科创50为什么只留了一个**：市面上有两只跟踪科创50指数的 ETF——华夏 588000 与易方达 588080，上交所给两只都挂了期权。但流动性差距悬殊，2026-09-18 实测：

| | 华夏 588000 | 易方达 588080 |
|---|---|---|
| 总成交额 | **16.7 亿** | 1.73 亿 |
| 有成交合约 | 169 / 170 | 148 / 164 |

588000 成交额是 588080 的 **9.66 倍**，且 588080 有 **16 个合约全天零成交**——零成交合约的价格只能是挂单价或结算价，**会污染 VIX 计算**。因此只保留华夏 588000。

> 如需恢复 588080：在 `vix.py` 的 `UNDERLYINGS` 里加回 `{"code": "588080", "name": "科创50ETF易方达", "tx": "sh588080", "market": "SSE"}`。历史 csv 里的旧记录仍保留，但 `history` 命令会自动隐藏已下线品种。

**未覆盖**：深交所其余 3 个（沪深300ETF 159919、中证500ETF 159922、深证100ETF 159901，前两个与已覆盖品种重复）与中金所 3 个股指期权（沪深300、中证1000、上证50）。

## 已知的不对称：深交所比上交所慢

深交所品种（目前只有 `CYB`）取数耗时明显更长——因为深交所官方接口**不提供价格**，需要先拉 24 页合约清单，再向新浪批量查价。实测：

- 上交所 5 个品种：约 10 秒
- 加上创业板后：约 16–40 秒（视网络）

这是数据源结构决定的，不是 bug。只查上交所品种时不会变慢。

## 怎么读这些数

**VIX 绝对水平**（按 A 股自身历史校准，见下方依据）：

| 区间 | 判读 | 含义 |
|---|---|---|
| < 13 | **极度平静** | 低于历史常态区间下沿，期权便宜到没人买保险 |
| 13–18 | **常态** | A 股 80% 的时间在这里，没什么好解读的 |
| 18–21 | **偏谨慎** | 接近停发前区间上沿，开始有人买保护 |
| 21–30 | **紧张** | 已超出停发前全区间，避险需求明显 |
| 30–40 | **恐慌** | 通常出现在急跌中 |
| ≥ 40 | **极度恐慌** | 股灾级别，往往对应阶段性底部区域 |

**分档依据**（重要，改阈值前必读）：官方中国波指 iVIX 停发前（2016-08 ~ 2018-02）的统计——均值 **15.19**，全区间 **11.15 ~ 21.41**，**80% 的时间落在 13.07 ~ 17.61**。

> 早期版本照搬美股习惯（12–16 平静、16–20 中性），是个错误：A 股波动本身更大，那套阈值会把 A 股最常见的区间误标成「平静」。2015 年股灾实测参照：4 月顶 47、6 月顶 49、7/8 暴跌后 60、8/26 最低点 65+。

**局限**：上述实证来自 2015–2018 年，市场结构已变化（期权品种从 1 个增至 6 个）。这是「更合理的参考」，不是精确标准。真正的解法是用本 skill 自己的历史序列算分位——需要 3–6 个月数据积累。详见 `references/methodology.md` 第九节。

**用相对关系做判断，比看绝对值更有价值：**

- **品种间比较**：50ETF 是权重蓝筹、中证500/科创50 是高波动成长。若 500ETF 与 50ETF 的 VIX 差值走阔，说明风险偏好在向小盘/成长集中挤压。
- **VRP 符号**：`VIX − RV20 > 0` 说明期权比实际波动贵（收租策略有溢价、市场在买保险）；转负则罕见，多出现在恐慌急跌之后。
- **期限斜率**：近月 IV 高于远月（斜率负）是常态，代表短期风险定价更高；若远月显著高于近月，说明市场担忧中长期不确定性。
- **行权价数低于 8 个**：该月份行权价覆盖过窄，方差不分低估，该期限的 IV 需打折参考。

## 数据源与方法

**上交所品种**
- **主源**：上交所官方接口 `query.sse.com.cn`，一次返回当日全部 694 个 ETF 期权合约（含行权价、认购认沽、收盘价、精确到期日、标的收盘价）。免费、权威、不限流。

**深交所品种**
- **元数据**：深交所官方接口 `szse.cn/api/report/ShowReport/data`（`CATALOGID=ysplbrb`），分页返回全部 464 个合约的行权价、认购认沽、到期日 —— **但不含价格**。
- **价格**：新浪财经 `hq.sinajs.cn`，批量查询（每次 50 个），取**字段 `[2]` = 收盘价**。
  ⚠️ 该字段下标是用上交所官方收盘价批量校验出来的（118 个合约全部精确吻合、零误差）。**改动前必须重新做这个校验**，否则会静默算错。

**通用**
- **已实现波动率**：腾讯财经日线，按 VIX 的数据日期对齐取 20 个交易日（见 `methodology.md` 坑 3）。
- **方法**：CBOE / 上交所《上证50ETF波动率指数编制方案》方差互换法。两条硬规则——剔除剩余 ≤ 7 天合约；近月 > 30 天时按编制方案注 3 直取近月。

编制细节、与官方的全部口径差异、验证记录与已知局限，见 `references/methodology.md`。**对外输出或用于决策前，务必先读其中第三节（口径差异）和第七节（已知局限）。**

## 上交所数据几点更新？

**先说结论：不知道确切时点，上交所未公开，接口里也没有时间戳（`TIMESAVE` 只有日期）。**

到 2026-09-18 为止只有这几次观测：

| 观测时刻 | 拿到哪天的数据 | 说明 |
|---|---|---|
| 9/17 21:37 | 9/17 | 当时已更新 |
| 9/18 21:17 | 9/17 | **尚未更新** |
| 9/18 21:27 | 9/18 | 已更新 |

两次观测把发布窗口夹在 **21:17–21:27** 之间。**但样本只有两次，不能当规律用。**

### 建议怎么跑

| 目标 | 做法 | 代价 |
|---|---|---|
| **确保拿到，绝不丢数据** | 次日早上跑（**当前定时任务 08:35 就是为这个设计的**） | 页面上显示的是前一交易日的数值 |
| 想当晚就看 | 21:30 之后再跑 | 基于现有观测的经验值，**不是保证**；万一某天发布更晚会漏 |

**别去赌时间点。** 脚本会自动比对「数据日期」与「市场最后交易日」，滞后时在终端和页面上明确提示——所以任何时点运行都不会把旧数据当新的给你。

### 让观测自动积累

每次运行都会往 `data/fetch_log.csv` 追加一条（运行时刻 / 数据日期 / 市场最后交易日 / 是否滞后），同 5 分钟内自动去重。

```bash
python3 vix.py probe     # 汇总分析
```

输出会列出「每个数据日期最早被观测到的时刻」——那就是**更新时点的上界**（数据在该时刻前已发布）。积累到 5 个以上数据日期后，就能给出有实据的运行时间建议，而不是靠猜。

## 手机端页面

`build_html.py` 生成 `output/vix_report.html`——**自包含单文件**（约 12KB，零外部依赖、无 CDN、无 JS 框架），手机浏览器打开即可看，弱网/离线也能渲染。

```bash
python3 build_html.py                    # 实时请求上交所接口后生成
python3 build_html.py --from-history     # 离线：用历史序列最后一天快照（接口故障时兜底）
python3 build_html.py -o /path/to.html   # 指定输出路径
```

页面内容：数据日期、波动最高/最低品种、5 张品种卡片（大字 VIX + 情绪标签 + 标的收盘 + RV + VRP + 期限斜率 + 期限结构迷你折线图）、以及口径免责声明。

**页面顶部有醒目的「非官方指数」声明**，这是有意为之——页面上没有上下文解释，脱离声明的裸数字容易被误读。

> 已接入定时任务：每个工作日 16:30 与数据采集一并在后台重新生成，无需手工操作。

## 历史序列与分位

**两条路径，各有用途：**

**① 自算序列**（`data/vix_history.csv`）——本工具逐日积累，口径与上面所有计算一致。
上交所接口只给当日快照，**历史无法回溯**，只能每天跑一次慢慢攒。

```bash
python3 vix.py calc --all --save     # 每日追加
python3 vix.py history               # 查看（样本 < 20 时不给分位结论）
```

**② 华创金工 HCVIX**（`data/hcvix_history.csv`）——外部长期参照，可立即使用。

```bash
python3 vix.py percentile            # 当前值在长期历史中的分位
```

- 来源：华创证券金工公开的 HCVIX 序列（页面内嵌 JSON），**2015-02-09 期权上市首日至今，单品种约 2800 个交易日**
- 覆盖：**仅 50ETF 与 300ETF 有对应序列**；创业板、中证500、科创50 暂无长期参照
- **引用必须注明来源：华创证券金工 HCVIX**

**独立验证价值**：2026-09-18 华创 HCVIX50 = **14.292**，本工具自算 50ETF = **14.2825**，差 0.08%。这是第三方独立编制结果对本工具算法的验证——**说明方差互换实现是对的**。

> 局限：HCVIX 是华创的编制口径，与本工具自算存在细节差异（300ETF 上差约 1.3%）。分位数值是「华创口径的历史位置」，不是「自算口径的历史位置」。自算序列积累够了之后，应以自算序列为准。

**③ 波动水平分位（RV 口径）**——覆盖全部品种，作为无 IV 历史时的过渡参照。

`percentile` 命令会同时输出第二张表：**20 日已实现波动率**在近 3 年（781 个交易日）中的分位。

```bash
python3 vix.py percentile        # 同时输出 VIX 分位（IV 口径）与波动水平分位（RV 口径）
```

> ⚠️ **RV ≠ IV，不可混称。**
> - `VIX 分位`：期权**隐含**波动率的历史位置 —— 市场**预期**未来怎么波动（向前看）
> - `波动水平`：标的**已实现**波动率的历史位置 —— 过去实际波动了多少（回头看）
>
> RV 分位能回答「现在波动水平在历史上算高还是低」，但**绝不能称作「VIX 分位」**。输出时必须带上口径标签。

**三个口径的分工**：

| | 覆盖品种 | 历史长度 | 口径 |
|---|---|---|---|
| 自算 VIX 序列 | 全部 | 逐日积累（当前 2 天） | IV，与本工具计算完全一致 |
| 华创 HCVIX | 仅 50ETF / 300ETF | 2015 至今（2814 天） | IV，第三方编制 |
| 波动水平（RV） | 全部 | 近 3 年（781 天） | **RV，不是 IV** |

### 原始期权链快照（重要机制）

`calc --save` 除写入 VIX 数值外，还会把**当日原始期权链**存到 `data/snapshots/YYYY-MM-DD.csv`（约 1100 条/天上交所+深交所全量）。

**为什么必须做**：VIX 数值一旦算出就固定了，但口径可能变（利率取值、行权价处理、算法修正）。没有原始链就无法重算。

**而历史无法回补**——已实测穷尽：

| 尝试 | 结果 |
|---|---|
| 东财历史K线（唯一能拿历史的源） | **每 3–4 个请求就封 IP**，恢复需数十分钟；拉长到 25 秒间隔仍失败。回补 250 个合约不可行 |
| 新浪 | 无期权历史接口（Service not found） |
| 腾讯 | 不支持期权代码（param error） |
| 上交所 | 只返回当日快照，`expireDate` 参数传值返回 0 条 |
| optbbs.com | 完全不可达（HTTP 000） |

**结论：错过即永久丢失。因此每日快照是唯一可靠的数据保全手段，不要去掉。**

> **已配置两个定时任务**（同一套脚本与数据，互补提高「不漏」的概率）：
>
> | 任务 | 时间 | 作用 | automation id |
> |---|---|---|---|
> | 「A股VIX每日采集」 | 工作日 **08:35** | **保底**：取前一交易日数据，此时数据必定已发布 | `6eb5da4b-51c5-4c72-a594-d1149a469a5b` |
> | 「A股VIX当日补采（兜底）」 | 工作日 **21:40** | **争取及时**：日终数据多在 21:00–21:40 发布，此时可补上当日数据 | `4183f55d-b366-4111-9201-1d435a81603a` |
>
> **为什么需要两个**：单条 rrule 不支持多时点（实测 `BYHOUR=8,16` 被拒）。而早上跑最稳（前一日数据必在）但慢一天；当晚跑及时但数据可能尚未发布。两个时点覆盖两种情形，去重机制保证不会重复记录。
>
> **上下限说明**：这两个任务能覆盖「电脑/应用当天有运行」的情况。若某天完全未运行，**该日数据永久丢失**——上交所只提供当日快照，无法回补。

> **缺口检测**：`history` 命令会自动比对交易日历（取自腾讯标的日线），发现缺失交易日会明确列出并说明无法回补。「静默缺数据」是比缺数据本身更危险的事。

**自算序列**样本不足 20 条时，`history` 会主动拒绝给分位结论；此时改用 `percentile`（HCVIX 长期参照），但必须说明数据来源与口径差异。

## 历史数据回补（三个品种没有长期序列时的唯一出路）

**问题**：中证500 / 科创50 / 创业板**没有第三方公开的 IV 历史序列**——华创 HCVIX 只做 50ETF 与 300ETF；清华五道口 CIMV 已停更（止于 2023-03）；期货日报只有加权 IV。官方 iVIX（000188）2018-02 停发。网传「000993 = 中证500波指」是**错误信息**（实为「全指信息」指数）。

两条腿走路：

### 1. 期货日报加权 IV —— 已接入页面

`scripts/fetch_qhrb_iv.py` 从东财新闻检索抓取期货日报的「期权市场」日报，提取各品种加权隐含波动率。

- 走**普通网页接口**（`search-api-web` + 文章 HTML），**不受 push2his 那套行情限流影响**
- 已拿到 12 个观测（2026-03-25 ~ 09-16），渲染为中证500／科创50／创业板三张卡片里的
  **「近半年位置」行**，与「波动水平」行同级并列。50ETF／300ETF 因已有华创 HCVIX 的
  长期分位，不重复显示此行（代码里用 `elif` 互斥：有华创数据用华创，没有才用期货日报）。
- **口径是加权 IV，不等于本 skill 的方差互换 VIX**，数值系统性偏高。页面上已明确标注，不可混读

```bash
python3 fetch_qhrb_iv.py --pages 60 --save
```

### 2. 东财回补原始期权日线 —— 分批进行中

`scripts/backfill.py` 拉历史期权日线 → `scripts/recompute.py` 重算历史 VIX（算法与 `vix.py` 完全一致，可与现有序列直接拼接）。

```bash
python3 backfill.py --status              # 看进度（断点续传，可随时中断重跑）
python3 backfill.py --market sse --limit 20
python3 recompute.py --save
```

**关键限制**：

- **东财是唯一提供 A 股期权历史日线的免费源，但限流严苛**。实测：间隔 1.5s 连发 15 个全部成功，紧接着继续请求则全部失败（HTTP 000）；静默 7 分钟后仍失败。镜像域名（`1.`/`2.`/`35.` 等）、完整浏览器 headers、移动端 UA **均无改善**。判定为按时间窗口的配额限流，**不存在稳定的安全速率**。
- 因此只能分批慢跑。已配置自动化任务「A股VIX历史回补（分批慢跑）」（id `d9bc1138-59f0-4472-a8b7-08be7c54fc58`），每 12 小时跑 20 个合约。
- 已按 `STRIKE_BAND = 0.12`（行权价在标的 ±12% 内）过滤合约——VIX 积分中权重 ∝ 1/K²，深虚合约贡献极小。请求数 378 → 236。
- **回补窗口只能覆盖最近 3–4 周**：VIX 需要「当日的近月 + 次近月」，而**已摘牌合约查不到**（东财搜索接口与代码段探测均为空）。

### 尚未解决

**深交所（创业板）的东财历史数据存疑**：secid `12.90007069` 经东财搜索接口确认无误（MktNum=12）、实时行情可用，但 kline 接口全部失败。需在解封后复验。

完整的数据源排除清单（新浪/腾讯/网易/同花顺/雪球/mootdx/yunhq 等逐条结论）见 `references/methodology.md` 第十节，**勿重复调研**。

---

## 常见追问的处理

- **「今天 A 股 VIX 多少」** → 跑 `calc --all`，报告 5 个品种并点明最强/最弱。
- **「市场是不是很恐慌」** → 不只看绝对值，结合 VRP 与品种间差值一起说。
- **「能不能用来做期权卖方/买方」** → 可以给 VRP 与期限结构的参照，但**不给具体交易建议**，只陈述数据特征。
- **「和美股 VIX 比」** → 说明口径不同（CBOE 用 SPX 期权、连续两个周期合约插值，覆盖更深），不可直接横向对比绝对值。
